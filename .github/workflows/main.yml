name: CI

env:
  AWS_REGION: eu-north-1
  ACC: AKIAWNHTHBQOOWPHR42M
  SEC: kAvx4WuhNplmPwgJQ4VkrEWWV6qcroOU7LprrzEg

on:
  push:
    branches:
      - main

jobs:
  testing:

    runs-on: ubuntu-latest

    steps:
    - name: testing
      run: echo "hello world"

    - name: aws ver
      run: aws --version

    - name: Update pack
      run: |
        sudo apt-get update
        
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'  # Укажите версию Node.js
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build

    - name: Display build output
      run: ls -l dist

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ env.ACC }}
        aws-secret-access-key: ${{ env.SEC }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Upload to S3
      run: |
        aws s3 sync dist/ s3://frontbuck/dist --delete

